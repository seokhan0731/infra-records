# 미래에셋증권 AI 페스티벌 - NCP 인프라 구축 기록

## 개요

|         |                                                                     |
|---------|---------------------------------------------------------------------|
| 기간      | 2026/08/28~2026/09/06                                               |
| 환경 (최종) | NCP Server · Ubuntu 22.04 LTS · 4 vCPU/16GB/200GB SSD               |
| 구성      | Docker Compose - nginx->FastAPI->PostgreSQL17+pgvector · Neo4j 5.26 |
| 담당 역할   | 3인 팀의 클라우드 인프라 전담 - 서버 준비 · 운영 및 명세에 따른 서버 스펙 및 DB 적재를 위한 인프라 구축    |
| 제약      | 총 크레딧 300,000원 · 평가 기간 중 서버 정지 불가                                   |

해당 프로젝트는 2026 미래에셋증권 AI 페스티벌 예선 제출을 위한 프로젝트로, 금융상품 Agent Product Finder를 주제로, 데이터(대회 제공 + 자체 수집)를 HyperCLOVA X 모델을
이용하여, 에이전트를 개발하는 프로젝트이다. 이에 따라, 주최 측에서 제공한 NCP 크레딧을 통해, 주최 측이 평가 기간동안 접속할 수 있도록 서버를 구성해야만 했으며, 덩달아 에이전트 루프에서 정보 검색을 위한
DB 또한 서버 환경에서 구축을 해야했다.

**해당 프로젝트는 인프라 구축과 운영을 맡은 첫 프로젝트이며, 해당 문서에서는 해당 과정에서 무엇을 판단했고 무엇을 놓쳤는지를 기록한다.**

## 문서의 결론

- **좁게 시작하는 것과 나중에 좁히는 것은 다르다.**
- **사고가 없는 것과 대비가 되어 있는 것은 다르다.**
- **했다는 것과 됐다는 것은 다르다.**

각 항목의 근거와 사례는 마지막 [정리](#정리-1) 에 있다.

## 목차

- [계정 · 권한 · 비용](#0-계정--권한--비용)
- [서버와 디스크](#1-서버와-디스크)
- [접근 통제](#2-접근-통제)
- [컨테이너 환경](#3-컨테이너-환경)
- [사전 검증](#4-사전-검증-smoke-test)
- [운영 안정성](#5-운영-안정성-확보)
- [배포 후 확인](#6-배포-후-확인)
- [정리](#정리-1)

---

## 0. 계정 · 권한 · 비용

### 0.1 계정 구조 설계 의도

팀리더(B)의 루트 계정으로 크레딧이 지급 되었고, 루트 계정 외 서브 계정 3개를 생성하였다.  
총원이 3명이었기에, 루트 계정 + 2개의 서브 계정의 구성을 택할 수 있었지만, 루트 계정은 온전히 크레딧만을 관리하도록 책임 분리를 하기 위해, 루트 계정 제외 서브 계정 3개의 구성을 택했다.

### 0.2 권한 부여 설계 의도

모든 서브 계정의 접근 권한은 콘솔 및 API 접근을 모두 허용하였으며, 그룹 정책을 선적용 후, 필요에 따라 개별 정책을 부여하는 방식으로 운영하고자 하였다.
그룹 정책의 기본값으로는 NCP_ADMINISTRATOR와 NCP_FINANCE_MANAGER가 존재하는데, 본인(C)을 포함한 팀원 모두 dev라는 그룹 생성 후, NCP_ADMINISTRATOR 정책에
적용하였다. 개발 일정이 촉박하여, 개별 정책 목록까지 검토할 여유가 없었고, 화면에 제시된 두 기본값 중에서 선택하였다. NCP_FINANCE_MANAGER를 그룹 정책으로 설정하지 않은 것은, 주어진 크레딧이
존재하는 상황 속에서 팀원들이 실수로 Billing API에 접근하여, 크레딧을 소진할 가능성을 원천 차단하기
위해서였다.

#### 당시 판단에서 잘못된 부분

NCP_FINANCE_MANAGER는 권한을 더하는 정책이 아니라 청구 · 결제 메뉴만 접근할 수 있도록 좁히는 정책이다. 부여하지 않았다고 해서 권한이 차단되는 것은 없다. 또한 크레딧은
FINANCE_MANAGER를 선택한다 하더라도, 결국 서버와 스토리지 등 리소스 생성 및 운영에 의해 소진되며, 그 권한은 NCP_ADMINISTRATOR로 전원에게 부여된 상태였다. 즉 본래의 의도와는
다르게 소진과 무관하게 동작하도록 권한을 부여하였으며, NCP_ADMINISTRATOR 권한을 부여함으로써, 서브 계정이 루트 계정과 동등한 효력을 갖게 되기에 0.1에서의 루트 계정을 크레딧 관리 전용으로 분리한
구조도
해당 권한 부여에서 위배되었다.

#### 대안

별도 관리형 정책을 보다 보면, NCP_INFRA_MANAGER가 존재한다. 모든 서비스에 접근할 수 있으나 청구 및 결제 관리 메뉴만 차단되는 것이 이 정책에 해당하는데, 해당 정책을 적용하였다면, 0.1의 책임
분리를 그대로 유지할 수 있었을 것이다. 그룹 정책의 기본값으로 존재하지 않고, 별도 관리형 정책에서 검색하여만 확인할 수 있다.  
다만 개별로 부여하는 것만으로는 해결되지 않는다. NCP의 권한은 합집합의 개념으로 개별, 그룹, 역할 정책이 종합되어 권한을 부여받기 때문에, 사실 NCP_ADMINISTRATOR를 그룹 권한으로 부여받은 순간,
이미 개별 정책이 의미가 없어진다. 즉, 이번 기회를 통해, 권한은 합집합의 개념이고 Deny만 별도 예외 적용이 된다는 것을 확인하였으며, 좁게 시작해서 필요할 때, 넓히는 것이 올바른 운영 방향이라는 것을 깨닫게
되었다.

**실제 운영 기간 중 권한으로 인한 문제는 발생하지 않았다.**

### 0.3 비용 산정과 알림

#### 비용 산정

NCP 콘솔 요금 계산기 기준으로 운영 예상 비용을 산정하였다. 당시 공지된 평가 기간은 9월 한달 전체이기에, 30일 24시간 상시 가동을 전제로 계산하였다. 모두 VAT 별도 기준이다.

| 항목           | 조건                             | 금액           |
|--------------|--------------------------------|--------------|
| Server       | 4 vCPU · 16GB · 200GB 30일 24시간 | 193,240원     |
| 공인 IP        | 1개                             | 4,032원       |
| 익스플로러 임베딩 v2 | 4억 토큰 (임베딩 1회 기준)              | 80,000원      |
| HCX-005      | 평가 및 자체 시험                     | 10,000원      |
| **합계**       |                                | **287,272원** |

서버 항목은 직접 산정하였고, HCX 항목은 로컬에서의 답변 생성 및 테스트에 이용된 HCX-005 사용량과 청구된 금액으로 단가를 계산하여 보수적으로 합계에 반영하였다. 임베딩의 예상 비용은 모델 선정 담당이
산정한 값을 받아 계산하였다.

#### 알림

NCP 콘솔 Budgets에서 임계값 70% 알림을 초기에 설정하였으며, 이에 대한 근거는 별도로 존재하지 않는다.  
알림은 한 번도 발송되지 않았으며, 따라서 설정한 알림이 실제로 동작하는지는 확인되지 않았다.

#### 실제

2026/09/19 기준 누적 사용액은 약 110,000원으로 산정액의 약 38% 수준이다. 산정액의 38% 수준인 이유는 두 가지이다.

- 기존 계획했던 임베딩 모델을 다른 임베딩 모델로 대체하였기에, 해당 비용이 NCP에서 발생하지 않는다. 즉, 해당 비용이
  사라진 것이 아니라 별도 다른 플랫폼의 계정으로 이동한 것이다. NCP Budgets 는 NCP 내부 비용만 집계하므로 이 이동은 콘솔에 나타나지 않는다. 산정액의 28%에 해당하는 항목이 감시 범위 밖으로 나간
  것이며, 해당 공급자의 무료 범위에서 처리되어 문제가 되지 않았을 뿐이다.
- 산정은 30일 가동 기준이나 현재 풀스펙 가동 15일째이므로 아직 그 기간이 경과되지 않았다.

---

## 1. 서버와 디스크

### 1.1 초기 서버 사양 결정

**Ubuntu 22.04 LTS · 2 vCPU/8GB/50GB**

DB 적재가 이루어지지 않은 시점, 공인 IP와 Docker 컨테이너에 실행한 FastAPI 정상 작동 확인용으로, 비용을 절약하고자 2VCPU/8GB로 서버 스펙을 결정하였으며, DB 적재를 별도로 한다는
가정하에, 이미지로 빌드한 소스코드 정도의 용량을 여유롭게 사용하기위해 스토리지 크기는 50GB로 결정하였다. 처음에는 20GB를 고려했으나, Docker 이미지 레이어 · 빌드 캐시 · 컨테이너 로그가 누적되면서
소스코드 용량만으로 예상한 것보다 커진다는 점을 고려해 여유를 두었다. 실제 사용량을 측정해 정한 값은 아니며, 부족한 쪽보다 여유 있는 쪽을 택했다.  
운영 중 측정한 결과 Docker가 사용하는 디스크 크기는 1.5GB이었다. DB 데이터는 bind mount로 컨테이너 밖에 두었으므로 포함되지 않는다. 즉, 20GB로도 충분했으며 50GB는 과한
선택이었다는 것을 확인할 수 있었다.

### 1.2 최종 서버 사양 결정

**Ubuntu 22.04 LTS · 4 vCPU/16GB/200GB**

적재 직전인 9월 5일에 변경하였으며, 사양 변경은 서버 정지가 필요하고 평가 기간에는 정지할 수 없으므로 이 시점이 마지막 기회였다.

스펙업은 팀원이 공유한 명세에 따랐다. 명세 내용으로는 사전 변환 산출물과 비공개 입력의 해제 크기 합계가 약 23.32GB이고, 여기에 추가로 인덱스, 로그, 백업이 공간을 쓰므로 200GB를
제시하였다. 또한 명세는 이 사양을 표본 적재 후 조정할 시작점으로 명시하였다.

적재 완료 후 디스크 사용량은 197GB 중 66GB(35%)였다 (df -h /). 구성은 다음과 같다 (du -sh).

| 경로                     | 용량    |
|------------------------|-------|
| /srv/festival/db       | 31GB  |
| /srv/festival/prepared | 14GB  |
| /srv/festival/input    | 9.2GB |
| 전송용 압축본(*.tar.gz)      | 3.7GB |
| /var/lib/docker        | 1.5GB |

해제 크기 23.32GB 대비 실제 사용량은 약 2.8배였다. 원본 데이터 크기만으로 디스크를 산정하면 부족하다는 명세의 판단은 맞았고, 200GB는 충분했다.

다만 위 수치는 모두 운영이 끝난 뒤의 값이다. 적재나 평가가 진행되던 시점의 자원 사용량은 기록하지 않았다. 따라서 이 사양으로 충분했다는 것은 말할 수 있으나, 최소 얼마가 필요했는지는 알 수 없다.

명세는 표본 적재 후 조정을 전제하였으나 실제로는 전량 적재를 바로 진행하였다. 사양 조정에는 실측이 필요한데 실측은 적재 이후에 나오고, 정지가 필요한 사양 변경은 적재 직전이 마지막 기회였다. 적재가 끝난
시점에는 마감이 임박해 변경을 검토할 여유가 없었다. 결과적으로 조정 단계는 수행되지 않았다.

---

## 2. 접근 통제

### 2.1 ACG 설정

#### 인바운드 설정 (초기, 최종 동일)

| 프로토콜 | 접근 소스     | 허용 포트     |
|------|-----------|-----------|
| TCP  | 0.0.0.0/0 | 80 (HTTP) |
| TCP  | 0.0.0.0/0 | 22 (SSH)  |

0.0.0.0/0은 모든 IP 소스에서의 접근 허용을 뜻하는데, 초기에 이를 각 포트를 이렇게 세팅해놓은 이유는 다음과 같다.

- **80번 (HTTP)**  
  서버 구축 시점에는 주최 측 발신 IP가 공지되지 않았다. 제한할 대상을 알 수 없었으므로 전체 개방을 선택했다.  
  이후 발신 IP가 공지되었으나 규칙을 변경하지 않았다. 이유는 두 가지이다. 주최 측 공지가 여러 번 바뀐 전례가 있었기에, 공지된 주소 하나만 허용했다가 다른 발신 IP로 변경되고, 이 공지를 놓치는 경우,
  평가 자체가 실패한다. 또한 팀이 여러 네트워크에서 접근을 확인해야 했다. 80번 포트를 /32 로 제한했다면 팀원들이 각자의 위치에서 확인하는 것 자체가 불가능했다.

- **22번 (SSH)**  
  초기에 개발 일정이 촉박하여 네트워크 구성을 별도로 검토하지 않았다. NCP가 발급하는 관리자 비밀번호가 무작위 문자열이므로 추측과 단순한 매크로로는 접근하기는 어렵다고 판단하였기에, 별도 SSH에 대한 인바운드
  규칙은 고려하지 않았다.

#### 아웃바운드 설정 (초기, 최종 동일)

| 프로토콜 | 접근 소스     | 허용 포트   |
|------|-----------|---------|
| ICMP | 0.0.0.0/0 |         |
| UDP  | 0.0.0.0/0 | 1-65535 |
| TCP  | 0.0.0.0/0 | 1-65535 |

서버는 Docker 이미지 내려받기, HCX 호출, 패키지 설치 등을 위해 외부로 나가야 했다. 이들은 모두 도메인 기반이고, CDN을 거치는 경우 IP가 고정되지 않아 아웃바운드 규칙을 설정하는 것에 어려움이
있다. 이에 따라, NCP 아웃바운드 기본값을 그대로 두었다.  
또한 주최 측 공지가 요구한 우리측 서버 -> 주최 측 공개망 통신이 정상임을 헬스체크 요청을 통해 확인하며, 아웃바운드가 실제로 동작하는지 확인하였다. 추가적으로, server_time이 서버 시각과 일치해 시각
동기화에도 이상이 없는 것을 확인할 수 있었다. 다만 이 확인은 서버에서 밖으로 나가는 아웃바운드 설정을 확인한 것이지, 외부에서 우리 API로 들어오는 인바운드를 확인한 것은 아니다.

```bash
$ curl -s http://<주최 측IP>/health
{"status":"ok","server_time":"2026-09-05T21:06:16+09:00","notice_version":"2026-09-04"}
```

### 2.2 외부 SSH 접속 시도 및 대응

#### 다수의 외부 SSH 접속 시도

2.1에서 22번 포트에 대해 제한 없이 열어두었기에, 서버 생성 직후부터 무차별 접속 시도가 존재했다. 실제로 NCP쪽에서 SSH Bruteforce 유형으로 공격이 지속적으로 들어오고 있다고, 알림을
받았었다. 실제로 지금 시점에 명령어를 통해 확인한 외부 접속 시도는 다음과 같다.

- 기간 및 건수

| 기간       | 건수         |
|----------|------------|
| 8/29     | 1,993건     |
| 8/30~9/5 | 5,788건     |
| **합계**   | **7,781건** |

- 시도된 계정명 및 건수

| 계정명     | 건수  |
|---------|-----|
| admin   | 326 |
| user    | 188 |
| test    | 134 |
| support | 110 |
| ubuntu  | 101 |
| ubnt    | 95  |

#### 1차 대응 (fail2ban)

대다수, 인바운드 규칙을 설정하지 않은 22번 포트에 시도되는 매크로성 공격이고, NCP가 발급해준 무작위의 문자열 비밀번호가 뚫릴 가능성은 거의 없지만, 가능성 자체를 확실히 줄이고자 1차적으로 fail2ban을
적용하였다. fail2ban은 설정 파일에 [sshd] enabled = true 만 지정하였고, 나머지는 기본값(maxretry 5회, findtime 600초, bantime 600초)을 사용하였다. 이에 따라,
무차별 대입의 반복 속도를 제한하였다. 실제로 800건을 차단한 것을 로그를 통해 확인하였다.

#### 2차 대응 (공개키 전환 및 패스워드 접근 차단)

1차 대응 이후로도, 팀리더(B)가 추가 보안을 요구하였기에, 별도의 대응 방안을 마련을 했어야 됐다. 두 가지 방안 중에, 고민을 했었는데, 한 가지는 ACG 인바운드 규칙을 특정 IP로 정의하는 것이었고,
나머지는 공개키로 접속 방식을 전환하여 패스워드 접근 자체를 차단하는것이었다. 결론적으로는 공개키 전환 방안을 선택했으며, 이유는 다음과 같다.

ACG 인바운드 규칙을 통해, 접근 IP를 /32로 제한을 하게 되면, 작업 장소에 따라 IP가 달라지는 경우 유연하게 대응하기 어렵다는 점이다. 대개 주로 작업하는 집 또한 거의 IP가 바뀌지는 않지만, IP가
바뀔 가능성이 존재하고, 팀원들이 작업하는 장소의 IP를 모두 인바운드 규칙에 정의해야 하기에, 개발 일정이 촉박한 상황 속에서, 이는 너무 부적절한 조치로 판단하였다.

이러한 이유로, 공개키 전환 방식으로 패스워드 접근 자체를 차단을 하고자 하였다. IP 변경이 있더라도, 키가 등록된 기기에서는 패스워드 없이 접근 가능하기에, DB 적재를 위해 접근해야 하는 팀원과 본인의 공개키만
서버에 등록하면 되기에 인바운드 방안보다는 더 현재 시점에 적절한 방안이었다고 판단했다.

#### 공개키 적용 및 검증

- **공개 키 적용**

키는 각각 로컬에서 생성하고, 공개키만 서버에 등록하였다.  
이 당시, 팀리더(B)가 DB 적재를 위해 서버에 tar 파일을 전송해야했기에, 별도 계정(finagent) 또한 추가로 생성하였고, 같은 방식을 적용하도록 하였다.

```bash
# /etc/ssh/sshd_config.d/00-hardening.conf
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin prohibit-password
PubkeyAuthentication yes
```

파일명 앞에 00을 붙인 이유는 다음과 같다. sshd_config는 *.conf로 디렉터리의 파일들을 읽으며, 같은 항목은 처음 만난 값으로 확정된다. 서버 생성시, 기본값으로 50-cloud-init.conf가
존재하였고, 그 안에 PasswordAuthentication yes가 작성되어 있었기에 패스워드 인증 방식 자체를 차단하기 위해, 파일명 앞에 00을 붙여 이것보다 우선시 되게 하였다.

- **KbdInteractiveAuthentication**

패스워드로 접속하는 경로는 PasswordAuthentication 외에 KbdInteractiveAuthentication도 함께 비활성화해야 하기에, 00-hardening.conf에서 확인할 수 있는 것처럼
비활성화하였다.

- **검증**

설정 파일을 고친 뒤, 적용된 값을 확인하고, 공개키를 일부러 쓰지 않는 방식으로 접속을 시도해 보았다. 잘 적용되었음을 확인할 수 있었다.

```bash
$ sshd -T | grep -E 'passwordauthentication|kbdinteractiveauthentication|pubkeyauthentication'
pubkeyauthentication yes
passwordauthentication no
kbdinteractiveauthentication no

$ ssh -o PubkeyAuthentication=no root@127.0.0.1
root@127.0.0.1: Permission denied (publickey).
```

- **결과**

전환 이후 Failed password 기록은 0건이다. 9/6 이후 확인 시점까지 한 건도 없다. 공개키 전환 이후 fail2ban 의 역할은 크게 줄었다. 비밀번호로 접근할 수 없으므로 반복을 막는 것의 보안상
의미가
작아진다. 차단된 출발지가 sshd 에 도달하지 않아 연결 처리와 로그 적재가 줄어드는 효과는 남는다.

| 구분                     | 패스워드 인증시기 | 공개키 전환 이후     |
|------------------------|-----------|---------------|
| 인증 실패(Failed password) | 7,781건    | 0건            |
| 접속 시도                  | 계속        | 계속(하루 약 510건) |
| 패스워드 접속 성공             | 0건        | 0건            |
| fail2ban 차단            | 800건      | 0건            |

---

## 3. 컨테이너 환경

### 3.1 배포 전용 계정 (finagent)

컨테이너 운영에 root 계정을 사용하지 않기 위해 별도 계정을 생성하였다. 계정 생성과 공개키 등록은 SSH 하드닝과 같은 시점에 진행하였다.

| 항목   | 값                |
|------|------------------|
| 계정명  | finagent         |
| 셸    | /bin/bash        |
| 비밀번호 | 미설정              |
| 그룹   | finagent, docker |
| sudo | 미부여              |
| 인증   | 공개키              |

- **비밀번호 미설정 이유**

공개키로만 접속할 것이므로 필요하지 않다고 보았다.  
결과적으로 계정은 잠김 상태(passwd -S = L)가 되었고, 이는 sshd 설정과 별개의 층으로 작동한다. 이에 따라, sshd 설정이 되돌려지더라도 이 계정은 비밀번호로 접근할 수 없다.

- **docker 그룹 설정 이유**

명세서에 따르면 배포 스크립트가 docker compose를 sudo없이 호출하는 상태였기에, sudo 없이 컨테이너를 다룰 수 있게 하기 위해, usermod -aG를 통해 docker
그룹에 속하도록 하였다. docker 그룹에 속하면 Docker 데몬 소켓에 접근할 수 있어 sudo가 없어도 된다는 사실을 이용하기 위해서였다.

- **sudo를 주지 않은 이유**

컨테이너 실행에 필요한 권한만 부여하기 위해 sudo를 부여하지 않았다. 다만 docker 그룹에 속하도록 설정함으로써, 실질적으로 root 권한을 가진 것과 마찬가지이다.
Docker 데몬이 root로 실행되므로, 그룹 구성원은 호스트 파일시스템을 마운트한 컨테이너를 띄워 그 안에서 호스트 파일을 임의로 다룰 수 있기 때문이다. 따라서 sudo 미부여는 실수로 시스템을 건드릴 여지를
줄이는 조치이지 권한 자체를 제한하는 조치는 아니다.

- **확인**

```bash
$ id finagent
uid=1001(finagent) gid=1001(finagent) groups=1001(finagent),999(docker)
```

### 3.2 기존 배포 잔여물 정리

서버를 초기 구축 후, 서버에서 FastAPI가 정상적으로 동작하는지 확인하기 위해, 포트 80을 80->8000 포트 포워딩을 한 적이 있었다. 즉, 최종적으로 배포를 시도하면
nginx(80)와 충돌해 port is already allocated로 실패할 상태였다.  
또한 기존의 프로젝트 이름이 달랐다. Compose는 생성한 컨테이너 · 네트워크 · 볼륨에 com.docker.compose.project 라벨을 붙이고, down은 자신의
프로젝트 이름과 일치하는 라벨만 찾아 정리한다. 이름이 다르기에 서로 무관한 컨테이너로 시스템 입장에서 인식하고, 이 경우 docker compose ps 는 빈 목록을 반환하므로, 포트를 점유한 컨테이너를 찾으려면
docker ps -a 로 전체를 확인해야 한다.  
팀리더(B)가 배포를 진행할 예정이었으므로, 그 전에 해당 컨테이너를 docker rm -f로 제거하여 잔여물을 정리하였다.

### 3.3 데이터 경로와 소유권

#### 구조

코드와 DB 데이터를 다른 경로에 두는 구조는 팀 명세에 정의되어 있었다. 데이터는 bind mount 로 코드 디렉터리 밖에 둔다.

배포는 코드 package 를 /srv/festival/fin-213 에 다시 푸는 방식이다. 코드 디렉터리는 교체·권한 일괄 변경·삭제가 정상적으로 일어나는 영역이므로, 그 안에 DB 데이터가 있으면 코드를 다루는
작업이 데이터를 위협한다. 마운트 지점을 코드 영역 밖에 둔 이유로 추측된다.

#### 경로 준비

명세서가 요구한 6개 경로를 준비하고 소유권을 설정하였다.

```text
/srv/festival/            finagent finagent
├─ fin-213/               finagent finagent    코드 해제 위치
├─ input/                 finagent finagent    비공개 입력 해제 위치
├─ prepared/quality/      finagent finagent
├─ backup/                finagent finagent
└─ db/
   ├─ postgres/           finagent finagent
   └─ neo4j/              7474     7474        ← Neo4j
```

#### 컨테이너 실행 UID

bind-mount는 호스트 디렉터리를 컨테이너 안에 그대로 연결한다. 디렉터리는 하나이고 소유 UID 도 하나뿐이므로 호스트와 컨테이너가 같은 값을 본다. 따라서 그 UID 가 컨테이너 안에서 실행되는 프로세스의
UID 와 맞아야 쓰기가 가능하다. 그런데 컨테이너가 어떤 UID 로 실행되는지는 이미지마다 다르다.  
명세서에는 bind 경로의 소유권은 컨테이너가 쓸 수 있게 설정한다고만 작성되어 있었기에, 컨테이너의 UID는 이미지에서 직접 확인하였다.

```bash
$ docker run --rm neo4j:5.26-community id
uid=7474(neo4j) gid=7474(neo4j)

$ docker run --rm pgvector/pgvector:pg17 id
uid=0(root)
```

pgvector와 neo4j의 동작 방식이 달랐다. 따라서 db/neo4j만 호스트에서 미리 소유권을 맞춰야 했다.

- **pgvector**  
  root 로 시작해 데이터 디렉터리를 스스로 chown 한 뒤 권한을 내린다.
- **Neo4j**  
  UID 7474 로 실행되며 데이터 디렉터리를 chown 하지 않는다.

소유권을 설정하는 과정에서 chmod로 쳐야 할 것을 chown으로 입력해 /srv/festival 전체의 소유자가 숫자 750으로 바뀐 적이 있다. 단순한 입력 실수이지만 명령은 오류 없이
끝났다. 존재하지 않는 계정이어도 숫자 UID는 그대로 설정되기 때문이다. 명령 후 ls로 결과를 확인하는 습관 덕에 바로 발견해 finagent로 되돌렸고, /srv/festival/db/neo4j 디렉터리만
별도로 다시 7474로 설정하였다.

#### 명세서 오류 지적 (chown -R)

명세서 중, 실행 순서 문서에는 다음 명령이 있었다.

```bash
sudo chown -R "$(id -un):$(id -gn)" /srv/festival
```

$(id -un)은 접속한 계정이므로 finagent가 되고, -R이 하위 전체에 적용되어 올바른 권한을 설정해두었던 db/neo4j의 UID 7474도 함께 덮어쓴다. 그러면 Neo4j가 데이터 디렉터리에 쓸 수
없어 데이터 적재에 실패한다. 이에 경로와 소유권은 이미 준비된 상태이기에, 해당 명령을 건너뛰고 시작해달라고 요청하였다.

#### 명세서 오류 지적 (.env)

.env 작성은 팀리더(B)가 맡기로 확정되어 있었으므로, 본인은 Compose 파일이 어떤 변수를 사용하는지 확인하고 관련 문서를 대조하였다. 그 과정에서 실행 순서 문서의 .env 블록에 다음 두 줄이 없다는
것을
발견하였다. 다른 문서에는 있었으므로 해당 문서와 다른 문서가 어긋난 상태였다.

Compose 파일은 ${POSTGRES_DATA_PATH:-./.runtime/postgres} 형태이다. 변수가 없으면 기본값으로, ./.runtime/postgres로, 코드 폴더 안에 바인드가
된다.  
즉, 해당 값이 누락되었을 때 일어나는 일은 다음과 같다.

- DB 데이터가 코드 폴더 안에 생성된다.
- 존재하지 않는 bind 경로를 Docker가 root 소유로 생성한다.  
  -> neo4j(UID 7474)가 아니기에 적재에 실패한다.
- 준비해둔 /srv/festival/db/*가 사용되지 않는다.

이에 따라, 반영을 요청하였고, 실제 적재는 지정된 경로로 정상적으로 bind가 되었다.  
다만 저장소의 .env.example 에도 같은 취지의 주석이 있었으므로, 요청이 없었더라도 동일한 결과였을 수 있다.

**적재 후 소유자 및 권한 확인**

```bash
$ ls -lan /srv/festival/db
drwxr-x---  5 7474 7474 4096 Sep  6 05:10 neo4j
drwx------ 19  999 1001 4096 Sep  6 12:17 postgres
```

neo4j 는 설정한 UID 7474 가 유지되었고, postgres 는 UID 999 로 바뀌어 있다. pgvector 이미지가 root 로 시작해 데이터 디렉터리를 스스로 chown 한 뒤 해당 계정으로 내려가기
때문이며, 권한이 700 인 것도 PostgreSQL 이 데이터 디렉터리 권한을 스스로 맞추기 때문이다. 두 이미지의 동작 차이가 적재 후 상태로 확인되었다.  
별개로, 호스트에서 UID 999 는 lxd 계정에 해당하므로, -n 없이 조회하면 소유자가 lxd 로 표시된다. 같은 숫자를 호스트와 컨테이너가 각자의 계정 정보로 해석하는 것일 뿐이다.

---

## 4. 사전 검증 (smoke test)

### 4.1 사전 검증을 진행한 이유

명세에 따르면, 데이터 적재 작업은 대략 6시간 이상이 걸리는 작업이었다. compose 파일의 결함이 존재한다면, 컨테이너가 동작하지 못해 적재를 시작조차 할 수 없고, 원인을 찾는 동안 그만큼 시작이 늦어진다.
또한 제출 마감 기한 전날, 당일날에 적재 계획을 세워놨기에, 해당 작업이 컨테이너 동작상의 오류 등으로 지연이 된다면, 제출 자체를 못하게 되는 상황이었다. 더불어, 적재중 실패에 따른 체크포인트는 존재하지만,
매번 상태값만을 확인하는 작업은 기한이 얼마 안 남은 시점에서, 현실적으로 힘들다고 판단하였다.   
따라서, 결함을 찾는 비용이 가장 싼 지점은 적재를 시작하기 전 사전검증이라고 생각했고, 팀리더(B)가 전달한 docker-compose.db.yml로 DB 컨테이너를 먼저 띄워 보았고, 적재를 실패시킬 결함을
확인하였다.

### 4.2 NEO4J_PASSWORD

#### 컨테이너 띄우기 실패 (docker compose up -d)

```text
Failed to read config /var/lib/neo4j/conf/neo4j.conf: Unrecognized setting.
No declared setting with name: PASSWORD. Cleanup the config or disable
'server.config.strict_validation.enabled' to continue.
```

컨테이너 속 neo4j가 동작에 실패하였다. **docker compose up -d 자체는 오류 없이 끝났고, 원인은 컨테이너 로그에서 확인하였다.**

#### 원인

neo4j 이미지는 NEO4J_로 시작하는 환경변수를 설정값으로 번역한다. 접두어를 떼고 __를 _로, _를 .으로 바꾼다.
이에 따라, 그 당시 docker-compose.db.yml에 정의된 환경변수들도 이런식으로 번역된다.

```text
NEO4J_AUTH -> 번역 대상 X   neo4j 실행 전에, 소비되기에 문제 X
NEO4J_server_memory_heap_max__size -> server.memory.heap.max_size   실재 설정

NEO4J_PASSWORD -> PASSWORD  실재 설정 X, 컨테이너 동작 실패의 원인
```

Neo4j 5.x는 server.config.strict_validation.enabled가 기본값으로 true이기에, Neo4j가 모르는 설정을 무시하지 않고, 동작을 거부한다. 즉, 컨테이너가 동작 실패한 원인은
NEO4J_PASSWORD가 PASSWORD로 번역되지만, 이것이 실재하지 않는 설정값이기 때문이다. 즉 아래와 같이 NEO4J_ 로 시작하는 변수가 다섯 개였으나, NEO4J_AUTH 는 번역 대상이 아니고 메모리
설정 셋은
실재하는 설정으로 번역된다. 문제가 발생한 것은 NEO4J_PASSWORD 하나뿐이다. 접두어를 쓴 것이 문제가 아니라 번역 결과가 실재하는 설정인지가 문제였다.

```yaml
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:?Set a private strong password}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:?Set a private strong password}
      NEO4J_server_memory_heap_initial__size: ${NEO4J_HEAP_INITIAL:-1G}
      NEO4J_server_memory_heap_max__size: ${NEO4J_HEAP_MAX:-2G}
      NEO4J_server_memory_pagecache_size: ${NEO4J_PAGECACHE:-2G}
```

#### 해결 (변수 이름 변경)

컨테이너를 정상적으로 동작시키는 방안은 두 가지가 존재한다. 첫 번째는, strict_validation.enabled를 false로 바꾸는 방법이다. false로 바꾼다면, Neo4j가 모르는 설정을 무시하고,
동작을 진행하게 된다. 두 번째는, 기존 변수 NEO4J_PASSWORD를 여타 다른 변수로 변경하는 방법이다. 다른 변수로 변경하면, 번역이 되지 않고, 컨테이너 내부의 단순한 환경변수로 남게 된다.  
결국 두 번째 방안을 선택했는데, 그 이유는 다음과 같다. 첫 번째 방안을 선택한다면, 당장의 문제는 해결되지만, 여타 다른 NEO4J_server_memory_heap_initial__size 등과 같은 관련 설정
중에 오타가 생기면 Neo4j는 기본값으로 동작시킨다. 또한 사전 검증을 실시한 이유 자체가, 마감 기한을 지키기 위해, 필요한 잔오류는 다 해결하자는 것이었기에, 두 번째 방안을 선택했으며, 기존
NEO4J_PASSWORD를 CYPHER_PASSWORD로 바꾸어, 검증을 이어나갔다.

### 4.3 healthcheck 실패

4.2에서 언급한 것처럼, 변수 이름을 CYPHER_PASSWORD로 변경하였다. 그 결과, 컨테이너는 정상적으로 동작하였지만, healthy 판정이 나지 않았다.

#### 원인

healthcheck가 같은 비밀번호를 참조하고 있었기 때문이다. environment 쪽에서 변수명을 CYPHER_PASSWORD로 바꾸었지만, healthcheck 안의 참조는 놓쳤기에,
healthcheck는 더 이상 존재하지 않는 NEO4J_PASSWORD를 읽어 빈 비밀번호로 DB에 접속을 시도하게 된다. 이에 따라, 인증에 실패하기에 컨테이너는 정상적으로 뜨지만, healthy 판정이
나지 않았다.

#### 해결 (healthcheck에도 바뀐 변수명 적용)

바뀌기 이전 시점의 비밀번호를 참조하는 것이 문제였기에, healthcheck 속, NEO4J_PASSWORD도 CYPHER_PASSWORD로 동일하게 변경해주었다.

```yaml
    healthcheck:
      test: [ "CMD-SHELL", "cypher-shell -u neo4j -p \"$$CYPHER_PASSWORD\" 'RETURN 1' >/dev/null" ]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s
```

### 4.4 검증과 정리

#### 검증

4.3까지 수정한 구성으로 다시 컨테이너를 띄웠다. 이때, 실제 적재에 사용할 경로를 그대로 bind-mount하였다. 왜냐하면 경로 설정과 소유권(UID 7474)까지 함께 확인하기 위해서였다. 사전 검증의 결과는
다음과 같다.

```text
PostgreSQL · Neo4j 모두 healthy 판정
Neo4j UID 7474 로 databases · transactions · server_id 를 생성
```

#### 정리

검증을 마친 후, 생성된 데이터를 모두 삭제한 뒤, 소유권을 기존에 설정한대로 복구시켰다. 검증 과정에서 만들어진 부산물이 실제 적재에 섞이지 않도록 하기 위해서이다. 검증 과정에서 데이터를 실제로 적재한 것이
아닌, 초기화까지만 수행한 것이므로 본 적재에는 지장이 없었을 것이지만, 본 적재가 깨끗한 상태에서 출발한다는 것을 보장하기 위해 정리하였다.  
또한, 해당 적재 환경과 사전 검증을 진행 후, 결과를 팀리더(B)에게 전달하였으며, 실제로 본 적재에서는 팀리더(B)가 이 사항을 적용한 뒤 진행하였다.



---

## 5. 운영 안정성 확보

### 5.1 Docker 로그 회전

#### 로그 회전 설정 추가

```bash
# /etc/docker/daemon.json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "100m",
    "max-file": "3"
  }
}
```

컨테이너별, 로그 파일당 최대 용량을 100MB, 로그 최대 파일 개수는 3개로 설정하였다. 이에 따라, 4개의 컨테이너(nginx, api, postgres, neo4j)가 가질 수 있는 로그 파일의 총 용량은
1.2GB를 넘지 못한다.

#### 설정 이유

일반적으로 json-file 드라이버는 크기 제한이 없다. 데이터 적재를 수행하는 과정 중, 크래시 루프에 빠지면 같은 오류가 반복 출력되어 로그에 기록되고, 별도 제한을 걸어두지 않으면, 해당 로그들이 디스크를
점유하게 된다. 앞서 언급한 것처럼, 데이터 적재 일정에 차질이 생기면 안 되는 상황이었기에, 적재 전 사전에 설정 파일을 작성하여, 해당 가능성을 차단하고자 하였다.

#### 결과

```bash
$ docker inspect $(docker ps -q) --format '{{.Name}}  {{json .HostConfig.LogConfig}}'

/fin213-nginx-1  {"Type":"json-file","Config":{"max-file":"3","max-size":"100m"}}
/fin213-api-1  {"Type":"json-file","Config":{"max-file":"3","max-size":"100m"}}
/fin213-postgres-1  {"Type":"json-file","Config":{"max-file":"3","max-size":"100m"}}
/fin213-neo4j-1  {"Type":"json-file","Config":{"max-file":"3","max-size":"100m"}}
```

로그 설정은 컨테이너 생성 시점에 고정되므로, daemon.json 을 나중에 바꾸면 기존 컨테이너에는 반영되지 않는다. 이번에는 데이터 적재 및 배포 전에 설정해 두어 컨테이너가 처음부터 해당 설정으로
생성되었다.  
실제 데이터 적재 이후, 실제 로그 총량은 약 13MB로, 설정한 상한의 1% 수준인 것을 확인하였다. 해당 설정 파일이 없었더라도, 13MB 정도의 크기는 잔여 스토리지 용량과 함께 고려했을 때, 문제는
발생하지 않았겠지만, 평가 기간 중에는 디스크가 차더라도 대응할 여력이 없다는 점을 고려한다면, 상한을 사전에 적용해두는 쪽이 훨씬 현명한 선택이었다.

### 5.2 서버 재시작 정책

#### 필요한 이유

주최 측은 평가 기간 내, 서버와 API는 무조건적으로 켜두어야 된다고 공지했다. 이에 따라, NCP 쪽 점검 등의 사정으로 인해, 예기치 않은 서버 재부팅이 발생하더라도 사람의 개입 없이 컨테이너가
올라오도록 설정하고 검증을 할 필요성이 존재했다.  
이에 따라, 두 가지가 전제됐어야 했다.

```text
1. Docker가 부팅 시 자동 시작되는가    ->  systemctl is-enabled docker
2. 컨테이너에 재시작 정책이 존재하는가   ->  compose 파일의 restart 정책
```

1.이 보장되지 않는다면, 2.의 정책과 무관하게 아무것도 뜨지 않기에, 1.을 반드시 설정 했어야 했다.

#### 적용 및 검증

1.의 경우, 서버 초기 구축 단계에서, enabled로 설정을 해놓은 뒤, 실제 검증은 진행되지 않은 상태였다. 실제 검증은 서버 사양을 4 vCPU/16GB로 상향할 때, 진행하였다. 서버 사양을 올리는 과정에서
서버를 정지했다가 다시 켜야 됐다. 따라서 이 과정이 재부팅 검증이 되었고, 컨테이너 부팅과 동시에 자동으로 올라오는 것을 확인할 수 있었다.

```text
CREATED 5 days ago  /  Up 15 minutes
```

컨테이너가 5일 전에 만들어졌는데 가동 시간이 15분이라는 것은, 기존 컨테이너가 자동으로 재시작되었다는 뜻이다.

#### 재부팅 정책 검증 한계

다만, 앞서 검증한 것은 이전 테스트 버전의 컨테이너였으며, 실제 배포 구성은 해당 시점과 다르므로, 해당 재부팅 정책은 실제 배포가 끝난 뒤 확인해야 한다고 기록하여, 팀리더(B)에게 전달하였다.

#### 컨테이너 재시작 정책

| 항목       | 정책             |
|----------|----------------|
| api      | always         |
| postgres | unless-stopped |
| neo4j    | unless-stopped |
| nginx    | unless-stopped |

- **always**  
  수동으로 정지한 컨테이너도 재부팅 시 다시 올라온다.
- **unless-stopped**  
  수동으로 정지한 경우는 다시 올라오지 않는다.

두 정책은 평상시 재부팅에서는 동일하게 동작한다. 다만, 차이가 드러나는 경우는 하나다. 누군가 docker stop으로 컨테이너를 멈춰둔 상태에서 서버가 재부팅되면, api는 정상적으로 올라오지만,
unless-stopped의 세 컨테이너는 올라오지 않는다. 즉, nginx도 unless-stopped이기에, 정지된 상태에서 재시작이 되는 경우, 외부 접근 경로 자체가 열리지 않는다.  
이에 따라, 팀리더(B)에게 전달 받은 compose 파일에서 이를 발견하고 참고 사항으로 해당 내용도 전달하였다.

### 5.3 자동 재부팅 차단 확인

**5.2**가 재부팅에 따른 복구성에 관련된 내용이라면, **5.3**은 재부팅 가능성에 대해 다룬다.

#### 필요한 이유

서버와 요청 API는 평가 기간동안 무조건적으로 켜져있어야 했기에, 재부팅 여부도 확인을 할 필요성이 있었다.

#### 재부팅 가능성 확인 (Automatic-Reboot)

Ubuntu는 unattended-upgrades로 보안 업데이트를 자동 적용하며, 설정에 따라 업데이트 후 서버를 자동으로 재부팅할 수 있다. 즉, 해당 업데이트로 인해, 평가 기간 중, 서버가 임의로 내려갈
가능성이 존재한다. 재부팅 후 복구되도록 설정해 두었으나, 재부팅 자체가 일어나지 않는 편이 낫다. 복구 과정에서 예기치 못한 변수가 생길 수 있기 때문이다. 이에 따라, Automatic-Reboot의 설정값을
확인하였다.

```bash
$ grep -i "Automatic-Reboot" /etc/apt/apt.conf.d/50unattended-upgrades
//Unattended-Upgrade::Automatic-Reboot "false";
// when Unattended-Upgrade::Automatic-Reboot is set to true
//Unattended-Upgrade::Automatic-Reboot-WithUsers "true";
//Unattended-Upgrade::Automatic-Reboot-Time "02:00";
```

모든 라인이 주석 처리되어 있기에, Automatic-Reboot가 설정되지 않은 상태였다. 이 경우 기본값인 false로 동작하므로 업데이트 후 자동 재부팅이 일어나지 않는다. 별도 조치를 할 필요가 없는 것을
확인했다.

### 5.4 tmux 사용 제안

#### 필요한 이유

컨테이너를 띄우는 것과 컨테이너 안에서 명령을 실행하는 것은 다르다. 컨테이너는 SSH 접속이 끊겨도 계속 실행되지만, 컨테이너 내부의 명령은 그렇지 않다.

```text
컨테이너              Docker 데몬이 관리 → SSH 연결과 무관하게 계속 실행

systemd(PID 1)
 └─ dockerd (Docker 데몬)
     └─ 컨테이너 프로세스
     
docker compose run   명령을 실행한 셸의 자식 프로세스  → SSH 가 끊기면 부모 셸과 함께 종료됨
                     
sshd
 └─ bash (SSH 셸)
     └─ docker compose run     ← 컨테이너가 끝날 때까지 여기 붙어 있음
```

**부모 프로세스가 죽으면, 자식도 같이 종료되기에, SSH 연결이 끊기면, 컨테이너 안에서 명령을 실행할 수 없다.**

데이터 적재는 대략 6시간 이상이 소요되는 장시간 작업이었다. 하지만 적재 명령은 SSH 연결이 끊기면 함께 종료된다. 즉, 적재 과정에서 SSH 연결이 네트워크 등의 원인으로 끊기게 된다면, 재개는 가능하지만
마감이 임박한 상황에서 그만큼의 지연은 위험 요소였다.  
이에 따라, 터미널 세션을 SSH 연결에서 분리해 유지하는 tmux를 서버 환경에 설치 후, 팀리더(B)에게 tmux를 사용하는 방안을 제안하였다. 실제 적재는 팀리더(B)가 수행하였다.

---

## 6. 배포 후 확인

### 6.1 API 명세서와 실제 호출 응답 확인

명세서 전문은 대회 제출물이므로 이 기록에 포함하지 않는다. 본문에서 인용한 항목은 제출본의 내용 그대로다.

#### 배포 후, 실제 응답 확인이 필요한 이유

API 명세서는 주최 측에게 우리측 서버를 호출하기 위한 매뉴얼을 담은 문서이다. 평가 기간에는 문의하거나 수정할 기회가 없으므로, 명세에 적힌 내용이 실제 동작과 다르면 팀평가에 불이익으로 적용될 가능성이
존재한다.  
따라서 명세서는 응답 예측이 아닌, 실제 응답 기반으로 확인되어야 했다. 이에 따라, 배포 종료 후, 실제 응답을 확인했다. 해당 확인은 서버 밖에서 우리 공인 IP로 각 케이스에 맞는 요청을 보내는 방식으로
수행했다. 서버 안에서 확인하는 방법도 있었지만, 출발지가 서버 자신이 되므로, ACG 규칙이 적용되는 방식이 외부 호출과 달라질 수도 있기에, 서버 밖에서 확인하는 방식을 선택했다.

#### 확인 결과

nginx 의 경로 제한은 팀에서 설계한 구성이며, 여기서는 그 구성이 외부에서 실제로 그렇게 동작하는지를 확인하였다.

```bash
$ curl -s "http://<공인IP>/health"
{"status":"ok","version":"0.1.0"}

$ curl -s "http://<공인IP>/ready"
{"status":"ready","mode":"production","production_data_connected":true,"capabilities":15}

$ curl -s -o /dev/null -w "docs     %{http_code}\n" "http://<공인IP>/docs"
docs     404

$ curl -s -o /dev/null -w "answer/  %{http_code}\n" "http://<공인IP>/answer/"
answer/  404

$ curl -s -o /dev/null -w "POST     %{http_code}\n" -X POST "http://<공인IP>/answer?question=x&question_id=1"
POST     405

$ curl -s "http://<공인IP>/ready"
{"status":"ready","mode":"production","production_data_connected":true,"capabilities":15}
```

| 구분 | 경로           | 명세 사항                                      | 실제        |
|----|--------------|--------------------------------------------|-----------|
| 허용 | /answer      | 200 · 다섯 문자열                               | 200       |
|    | /health      | {"status":"ok","version":"0.1.0"}          | 명세 사항과 동일 |
|    | /ready       | {"status":"ready","mode":"production",...} | 명세 사항과 동일 |
| 차단 | /docs        | 404                                        | 404       |
|    | /answer/     | 404 (경로 완전 일치)                             | 404       |
|    | POST /answer | 405                                        | 405       |

명세서에 기술한 요청 제어는 /answer · /health · /ready만 허용하고, 그 외에는 404 응답을 반환하는 것이었다. 허용한 세 경로가 모두 열려있고 나머지가 차단된 것을 확인하였다.

- **/answer/**  
  /answer/는 끝에 슬래시가 붙은 경로로, nginx는 경로가 완전 일치될 때 매칭되기에 슬래시 하나로 다른 경로로 판단된다. 이에 따라 404를 반환한다.
- **/docs**  
  FastAPI는 기본으로 /docs에 API 스키마 문서를 노출한다. 정의된 엔드포인트와 요청 및 응답 구조가 모두 담기므로, 해당 요청을 열어두지 않는 것이 일반적이며, nginx에 의해 404를 반환한다.
- **/answer**  
  응답 필드는 아래에 있는 다섯 개의 필드이다.
    - question_id
    - question
    - retrieved_context
    - think_trace
    - answer

```bash
$ curl -G "http://<공인IP>/answer" \
    --data-urlencode "question_id=Q-TEST" \
    --data-urlencode "question=KODEX 200의 총보수는 얼마야" \
    -s -o /dev/null -w "status %{http_code}  time %{time_total}s\n"
status 200  time 2.035s
```

```json
{
  "question_id": "Q-TEST",
  "question": "KODEX 200의 총보수는 얼마야",
  "retrieved_context": "ORGANIZER_ZERO_POLICY",
  "think_trace": "[\"1 Query Understanding: LOOKUP / 상품군 ETF_KR / 분석기 HCX\", ...]",
  "answer": "주최 측 데이터의 0은 실제 값이 아니라 기준이 없다는 표시여서 순위에서 제외했습니다."
}
```

이 응답은 **데이터로 확인 불가**에 해당하는 응답이다. 오류가 아니라 200 과 다섯 문자열 필드로 응답하고 사유가 answer 에 담겼다. 정상 경로뿐 아니라 예외 경로에서도 계약이 지켜지는 것을
확인하였다. 응답 시간은 약 2.0초로, API 명세서의 목표 응답 시간인 60초 이내였다. 단순 조회 질의 기준이며 복잡한 질의는 다를 수 있다.

#### 확인에 따른 명세서 수정 사항

- **Content-Type**  
  명세 초안에는 응답 헤더를 application/json; charset=utf-8 로 기재했으나 실제 응답은 application/json 이었다. JSON 은 UTF-8 인코딩이 표준으로 정해져 있어
  application/json 에는 charset 파라미터가 정의되어 있지 않다. 다만 관행적으로 charset 을 붙이는 구현도 많아 흔한 형태를 기준으로 추정한 것이었다. 실제 응답을 확인한 뒤 charset
  표기를 제거하였다.
- **retrieved_context의 확인 불가에 따른 응답값**  
  명세 초안에는 확인 불가일 때 문자열 하나가 들어간다고 기재하였다. 로컬 검증 과정에서 한 건만 나온 것을 그대로 일반화한 것이다. 이후 다른 케이스로 실제 서버 응답을 확인한 결과, 제외 사유가 여러 건
  포함되어 줄바꿈으로 이어져 있었다. 명세를 여러 건이 들어올 수 있는 형식으로 수정하였다.

### 6.2 긴 질의에 따른 414 응답

#### 414 Request-URI Too Large

800자 분량의 자연어 질의에 대해서는 200이 반환되고, 3,000자 분량의 질의에 대해서는 nginx가 414 Request-URI Too Large를 반환하였다. 즉, 애플리케이션까지 도달하지 못하고,
nginx 선에서 필터링된 것이다.

#### 원인

질의는 요청의 쿼리 파라미터로 전달되는데, 이때 한글은 URL 인코딩시 1자가 9Byte가 된다. 이를 바탕으로 계산하면 3,000자 요청 줄은 약 27KB다. nginx의
large_client_header_buffers 기본값은 버퍼당 8KB이고, 요청 줄은 버퍼 하나에 들어가야 하므로, 한글 기준으로 약 900자까지 가능하다(8 × 1024 ÷ 9 ≈ 910).  
이는, 800자는 통과하고 3,000자의 질의는 거부된 측정 결과와 일치한다.

#### nginx 설정 수정 X

- **수정하지 않은 이유**  
  이를 해결하는 방안으로는 nginx의 large_client_header_buffers를 늘리는 방안이 있다. 하지만 택하지 않았다. 그 이유는 다음과 같다.

    - 평가 질의는 자연어 한 문장이므로 대개 300자를 넘지 않을 것이다.  
      -> 실제 평가에서 도달할 상황이 발생하지 않는다.
    - 마감이 임박한 시점에 nginx 설정을 수정하면 문법 오류로 80번 포트가 죽을 수 있다.  
      -> 변경 후 정상 의도된 동작을 하는지 검증할 시간이 없었다.

해당 긴 질의에 대한 이슈는 최종 문서화 이후, 제출 마감 30분 전에 수행되었기에, 수정하여 얻을 수 있는 이점보다는 리스크가 더 크다고 판단하였다.  
이에 따라, 발견한 제약을 명세서에 기록할지에 대한 결정을 해야했다.

- **명세서에 작성하지 않은 이유**
    - **당시 판단**  
      평가 범위에서 발생할 상황이 아니라고 보았고, 실패 조건을 나열하면 불필요한 제약으로 읽힐 것이라 판단했다.

    - **현 시점 판단**  
      다만 지금 시점에 돌이켜 보면 판단의 방향이 달랐어야 했다. API 명세서에는 요청률(초당 5회), 동시성(순차 1건), 운영 기간, 응답 시간, 그리고 응답 길이까지 보장 범위를 기재하였다. 그런데 요청
      길이만 언급이 없었다. 응답 쪽은 길이 제한은 없다고 다루면서 요청 쪽은 보장 범위를 다루지 않은 것이다.  
      어느 계층에나 한계는 있고, 그것을 모두 한 순간에 발견해 나열하는 것은 불가능하다. 적어야 했던 것은 실패 조건이 아닌, "질의는 자연어 한 문장 수준을 가정한다" 정도의 보장 범위였다. 그렇게 적었다면
      발견 여부와 무관하게 계약이 명확해졌을 것이다.

---

## 정리

이 장에서는 0~6장의 작업을 가로질러 나타난 것을 모았다. 각 장을 따로 쓸 때는 개별 판단으로 보였으나, 모아놓고 보니 세 가지 형태가 반복되고 있었다.

### 좁게 시작하는 것과 나중에 좁히는 것은 다르다.

#### 반복된 형태

| 행위                           | 결과                           |
|------------------------------|------------------------------|
| 그룹에 NCP_ADMINISTRATOR로 권한 부여 | 권한이 합집합이므로 개별 정책으로 좁힐 수 없었다. |
| 22번(SSH) 포트를 0.0.0.0/0으로 오픈  | 8일간 7,781건의 접속 시도를 받았다.      |

좁히는 것 자체는 어렵지 않다. ACG 규칙은 콘솔에서 수정하면 되고, 그룹 정책도 교체할 수 있다. 문제는 좁히기 전까지의 기간이다.

SSH 는 그 기간에 7,781건의 시도를 받았고, 실제로 뚫렸는지는 침해 흔적을 따로 점검해서야 확인할 수 있었다. 좁게 시작했다면 그 확인 작업 자체가 필요하지 않았다.

권한 쪽은 한 가지가 더 있었다. "그룹 선적용 후 개별 부여" 라는 운영 방식을 정해두고 그룹에 최대 권한을 주었으므로, 그 방식 안에서는 좁힐 수단 자체가 없었다.

#### 다시 한다면

그룹에는 청구·결제가 차단된 정책(Ex. NCP_INFRA_MANAGER)을 부여하고 필요한 사람에게만 개별로 추가한다. SSH 는 출발지를 제한한 상태로 시작한다. 접속 위치가 유동적인 경우 경유 서버나 VPN
으로 출발지를 통일하는 방법이 있으나, 당시에는 고려하지 않았다.

### 사고가 없는 것과 대비가 되어 있는 것은 다르다.

#### 반복된 형태

| 결과                                | 직접적 이유                                                       |
|-----------------------------------|--------------------------------------------------------------|
| 전체 권한을 팀원들에게 부여했으나, 문제가 발생하지 않았다. | 팀이 세 명이고, 기간이 짧았다.                                           |
| 22번을 열어두었으나 뚫리지 않았다.              | NCP에서 발급받은 관리자 비밀번호가 무작위 문자열이기에, 매크로성 공격으로는 뚫릴 가능성이 현저히 낮았다. |

두 번 모두 사고는 없었고, 그 이유는 조치가 아니라 조건에 있었다. 팀의 규모가 더 컸거나 기간이 길었다면, 비밀번호가 추측 가능한 것이었다면 결과는 달라졌을 수 있다.  
**따라서 이번에 사고가 없었다는 사실은, 다음에 같은 방식을 써도 된다는 근거가 되지 못한다.**

### 했다는 것과 됐다는 것은 다르다.

#### 반복된 형태

| 행위                             | 확인                          |
|--------------------------------|-----------------------------|
| 하드닝 파일 작성                      | sshd -T · 실제 접속 시도 -> 적용 확인 |
| chown 실행                       | ls -lan -> 오입력 발견           |
| docker compose up -d (스모크 테스트) | 컨테이너 로그 -> 동작 실패 발견         |
| API 명세서 초안 추정으로 작성             | 배포 후 외부에서 호출 -> 초안 두 건 수정   |
| 서버 재부팅 정책 설정 (컨테이너 관련)         | 구버전 구성으로만 확인                |
| NCP Budgets를 통해 비용 알림 설정       | 임계값에 도달하지 않아 확인 불가          |
| Docker 로그 상한 설정                | 상한에 도달한 적 없어 확인 불가          |

해당 사례를 가장 뚜렷하게 보여주는 것은 chown이다. chmod로 쳐야 할 것을 chown으로 입력했을 때, 명령은 오류 없이 끝날 수 밖에 없었고, ls로 확인하는 습관이 없었다면 그대로 넘어가 데이터 적재가
지연됐을 것이다.

#### 설정과 확인의 한계

설정과 확인은 계층 구조로 나눌 수 있다.

| 구분         | 확인 가능성     | 예시                             |
|------------|------------|--------------------------------|
| 설정이 적용되었는가 | 확인 가능      | sshd -T· docker inspect        |
| 설정대로 동작하는가 | 조건을 만들면 가능 | ssh -o PubkeyAuthentication=no |

공개키 설정은 두 층을 다 밟았다. 설정이 최종값이 되었는지를 sshd -T 로 보고, 실제로 막히는지는 공개키를 쓰지 않는 상황을 직접 만들어 확인하였다.  
반면 크레딧 비용 알림과 로그 회전은 설정에서 멈췄다. 로그 회전은 해당 문서를 작성하며 docker inspect로 적용 여부를 뒤늦게 확인하였고, 운영 중에는 확인하지 않았다. 비용 알림은 운영 기간 중 임계값에
도달한 적이 없어 발송 여부를 알 수 없었으며, 발동 조건을 따로 만들어 보지도 않았다.  
크레딧 임계값을 잠시 낮추거나 시험용 컨테이너에 로그를 쌓으면 공개키와 같은 방식으로 설정대로 동작하는지까지 확인할 수 있었다. 재부팅 복구도 마찬가지이다. 재부팅 복구는 배포 완료 후 실제 구성으로 확인이
필요하다고 전달하였으나, 동작 여부를 확인하지는 않았다.

**세 항목 모두 확인할 수 없었던 것이 아니라 확인하지 않은 것이다.**

### 이번에 확인한 것

이번 인프라 구축 및 운영 경험을 통해, 다음 프로젝트에서 적용할 수 있는 사실들이다.

|                                 |                                                                                  |
|---------------------------------|----------------------------------------------------------------------------------|
| 권한은 합집합이며 Deny 만 예외다.           | 그룹에 최대 권한을 주면 개별 정책으로 좁힐 수 없다.                                                   |
| 원본 데이터 크기만으로 디스크를 산정하면 부족하다.    | 해제 23.32GB -> 실제 사용 66GB. 원본을 보관한 채 DB · 인덱스 · 이미지가 별도로 쌓인다.                     |
| 컨테이너 실행 UID 는 이미지마다 다르다.        | neo4j 7474, pgvector root. 이미지에서 확인한다. (docker run --rm neo4j:5.26-community id) |
| Docker 로그 설정은 컨테이너 생성 시점에 고정된다. | 재시작이 아니라 재생성이 필요하다.                                                              |
| 컨테이너와 그 안에서 실행한 명령은 수명이 다르다.    | 전자는 SSH 연결이 끊겨도 유지되지만, 후자는 종료된다. -> tmux 사용                                      |
| 한글은 URL 인코딩 시 1자가 9Byte         | nginx 요청 줄 한계가 한글 기준 약 900자                                                      |

### 이번에 하지 못한 것

#### 백업과 복구

명세서가 요구한 데이터 적재 경로와 백업 경로를 준비하였다. 그러나 실제 백업과 복구 시험은 수행되지 않았고, 운영 종료 후 확인한 결과 백업 디렉터리는 비어 있었다. 백업은 양쪽 작업 절차(B/C) 어디에도
포함되어 있지 않았다. 누가 언제 수행하는지가 정해지지 않은 채로 넘어갔다.  
원칙이 있어도 절차에 자리가 없으면 수행되지 않는다. 다음에는 dump 를 뜬 뒤 다른 이름의 DB로 복원해 데이터를 대조하는 단계를 절차에 포함한다.

#### 운영 중 관측

자원 사용량을 시계열로 남기지 않아 사양 적정성 · 비용 추이 · 장애 징후를 사후에 확인할 수 없다. 서버의 최소 필요 사양을 알 수 없었던 것, Docker 로그 총량을 이 문서를 쓰면서야 처음 측정한 것,
NCP 크레딧의 비용을 일일 소비 속도로 보지 못한 것이 모두 같은 이유다.
지금 값만 보는 명령으로는 피크와 추세를 알 수 없다. 다음에는 디스크 · 메모리 사용량과 일일 비용을 주기적으로 파일에 남겨 피크와 증가 속도를 확인한다.

#### 구성의 재현

모든 구성을 콘솔과 명령으로 수동 수행하였다. 구성이 남아 있는 곳이 서버 자체뿐이므로, 이 문서를 쓰면서도 설정값을 확인하려면 서버에 접속해 다시 조회해야 했다. 서버를 반납하면 그 방법도 사라진다.
다음에는 명령으로 수행한 부분만이라도 스크립트로 남긴다.

---